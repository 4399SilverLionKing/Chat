{
  "name": "@chat-tools/cli",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@chat-tools/shared": "workspace:*",
    "commander": "^14.0.0",
    "execa": "^9.6.0",
    "smol-toml": "^1.4.1"
  },
  "devDependencies": {
    "tsx": "^4.20.3"
  }
}
